// ProgramsSection is no longer used on the homepage in the ednia-style layout.
// Programs are now accessible via the header nav and filter bar.
// Keeping as an empty export for compatibility.

export const ProgramsSection = () => {
  return null;
};
